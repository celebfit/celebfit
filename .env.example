# celebfit API 설정

API_PORT=8000
ALLOW_FALLBACK=true
ENABLE_SD=true
USE_GITHUB_PIPELINE=true

# --- 로컬 Mac ---
# MODEL_REPO_ROOT=/Users/leejiwon/Downloads/ConditionalImageGeneration

# --- RunPod (Pod 환경 변수로 설정, 로컬 .env 불필요) ---
# MODEL_REPO_ROOT=/app/model_repo
# HF_HOME=/data/huggingface
# WARMUP_ON_START=true

# --- Flutter 앱 (RunPod URL, 마이 탭에서도 설정 가능) ---
# flutter run --dart-define=API_BASE_URL=https://xxxxx-8000.proxy.runpod.net
